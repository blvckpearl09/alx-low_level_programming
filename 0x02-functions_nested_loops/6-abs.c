#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @r: r is an integer
 *
 * Return: r (Bingo)
 */

int _abs(int)
{
	if (r > 0)
	{
		return (r);
	}
	else if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}
}
